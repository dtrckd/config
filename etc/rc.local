#!/bin/sh -e
#
# rc.local
#
# This script is executed at the end of each multiuser runlevel.
# Make sure that the script will "exit 0" on success or any other
# value on error.
#
# In order to enable or disable this script just change the execution
# bits.
#
# By default this script does nothing.

# Fix non wanted sleep wakeup
#for dev in XHC0 XHC1 XHC3 XHC4 NHI0 NHI1; do
#    echo $dev > /proc/acpi/wakeup
#done


# Fix Amd Gpu causing linux app freeze in Lenovo Yoga laptop
# GPU Power Management
#echo "manual" > /sys/class/drm/card0/device/power_dpm_force_performance_level
# VPE fix
# echo "options amdgpu ip_block_mask=0xFFFFF7FF" | sudo tee /etc/modprobe.d/amdgpu.conf
# sudo update-initramfs -u
# # lsinitramfs /boot/initrd.img-$(uname -r) | grep -i amdgpu
#
# # IOMMU fix (try passthrough first - least aggressive)
# sudo sed -i 's/GRUB_CMDLINE_LINUX_DEFAULT="\(.*\)"/GRUB_CMDLINE_LINUX_DEFAULT="\1 iommu=pt"/' /etc/default/grub
# sudo update-grub
# sudo reboot


# Set Firewall
/home/dtrckd/bin/firewall-strict

# Reset docker
service docker restart

# Run Mongo
#docker run -d -p 27017:27017 -v /home/dtrckd/src/data/mongo-docker:/data/db mongo

## Run Vpn
#sysctl net.ipv4.ip_forward = 1 # enable forwardind for DNS
## redirect dns
#iptables -t nat -A OUTPUT -p udp --dport 53 -j DNAT --to 80.67.169.40:53
#iptables -t nat -A OUTPUT -p tcp --dport 53 -j DNAT --to 80.67.169.40:53
#iptables -t nat -A POSTROUTING -j MASQUERADE
##sysctl net.ipv6.conf.all.disable_ipv6=0 # disable ipv6
#openvpn --config  /etc/openvpn/aqn.conf --auth-nocache

# Docker CHAIN rul problem
#systemctl restart docker.service

# Mouse trick (xserver-xorg-input-synaptics)
#syndaemon -t -k -i 0.2 -d


exit 0
